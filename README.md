Ubuntu 14.04 prerequisites
-----------------------
```sh
sudo apt-get install g++ libncurses5-dev libprocps3-dev autoconf autoconf-archive
```

Initial setup
-------------
```sh
autoreconf -i
make
```

