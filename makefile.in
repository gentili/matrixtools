CXX = g++
CC = g++
MAKE = gmake
SHELL = /bin/bash

CFLAGS = -Wall -g

# General rules
all: $(ALL_TARGETS)

clean:
	@echo " "
	@echo "*** cleaning ***"
	@echo " "
	-rm -f $(ALL_TARGETS); rm -f *.o

%.o : %.cpp
	$(CXX) -c $(OFLAGS) $(CFLAGS) $(INCDIRS) $< -o $@
